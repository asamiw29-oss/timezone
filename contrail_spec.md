# Contrail 仕様書 ／ 時差・所要時間・クルーレスト・グルメ

最終更新: 2026-07-18

---

## 0. この文書の目的

本アプリ **Contrail** は、フライトで複数のタイムゾーンを移動する人（客室乗務員・運航乗務員・海外出張者）向けの時間計算ツール。出発地と到着地のタイムゾーンが異なっても正しい**所要時間**を出し、長距離便の**クルーレスト（交替休憩）**の割り振りや、着陸前の**到着前チェックリスト**も扱う。起動画面のタグラインは「**時差・所要時間・クルーレスト・グルメ**」で、最後の「グルメ」は姉妹アプリ Atlas との連携（§3.4 Mode D）への布石。

デザインは共有マップアプリ Atlas の世界観（くすみ系パレット・搭乗券モチーフ・スプラッシュ演出）を継承する姉妹プロダクト。単一HTMLで完結し、オフライン／PWA にも対応する。本文書は前半で現行仕様、後半（§4）で計算・共有ロジックの詳細をまとめる。

---

## 1. 概要

| 項目 | 内容 |
| --- | --- |
| プロダクト名 | Contrail（TIME タグ付きロゴ／時計＋軌道のアプリアイコン） |
| コンセプト | フライトの時差・所要時間・クルーレスト・グルメを搭乗券モチーフのUIで |
| 想定ユーザー | 客室乗務員・運航乗務員・海外出張者 |
| 構成 | 単一HTMLで完結（フロントのみ、サーバー・DB・外部APIなし）。PWA としても配布可 |
| モード | 入り口でルート選択 → Mode A（所要時間）／ Mode B（クルーレスト）／ Mode C（到着前チェックリスト）／ Mode D（Atlas 連携・準備中） |
| 認証 | なし |
| 対応端末 | PC・スマホ（レスポンシブ）。PWA（テーマ色 #1C2A3A、standalone、maskableアイコン） |
| オフライン | ①単一ファイルを保存して使用、②PWAとして一度開けばオフライン起動（§2.1） |
| 表示言語 | 日本語UI |

**設計思想**: タイムゾーン計算はブラウザ標準の `Intl` が内蔵する IANA タイムゾーンDBだけで完結させ、外部ライブラリ・API・サーバーを一切持たない。アプリを開けばすぐ動き、運用コストはゼロ。共有・オフラインも端末内で完結する。

---

## 2. アーキテクチャ・技術スタック

```
[ブラウザ (単一HTML)]
   ├─ UI: 入り口（ルート選択）→ Mode A / Mode B / Mode C / Mode D(準備中)
   ├─ 計算: Intl.DateTimeFormat による wall-time ⇄ UTC 変換（DST内包）
   ├─ 共有: 条件をURLに符号化（リンク）／ Canvas で PNG 生成（画像）
   └─ データ: 内蔵の都市マスタ（IATA + IANA）
```

| レイヤ | 採用 |
| --- | --- |
| 実装 | 単一HTML（HTML + CSS + Vanilla JS、ビルド不要） |
| タイムゾーンDB | ブラウザ標準の IANA（`Intl.DateTimeFormat`）。追加データ・ライブラリ不要 |
| フォント | IBM Plex Mono / IBM Plex Sans（未読込時はシステムフォントにフォールバック） |
| ホスティング | 静的ホスティングで可（Vercel 等） |

将来 Atlas と同じ Next.js プロジェクトへ組み込む場合は、計算ロジック（§4）とデータ（§5）をそのまま移植し、UI をコンポーネント化すればよい。

### 2.1 オフライン対応・PWA

2通りのオフライン運用をサポートする。計算・共有はすべて端末内で完結するため、いずれもネット接続なしで機能する。

| 配布形態 | 内容 | ファイル |
| --- | --- | --- |
| 単一ファイル版 | `contrail.html` を保存しダブルクリックで使用。ネット依存は見た目用フォントのみで、無ければシステムフォントに自動切替 | `contrail.html` |
| PWA版 | 一度オンラインで開けば、以降はオフライン起動でき、フォントもキャッシュ済みで Plex のまま表示。ホーム画面に追加可 | `index.html` ＋下記 |

**PWA 構成ファイル**: `index.html`（本体＋manifest/SW読込）、`manifest.webmanifest`（アプリ名・アイコン・テーマ色・standalone）、`sw.js`（Service Worker：アプリ本体とフォントをキャッシュ。ナビゲーションはネットワーク優先→キャッシュフォールバック）、アイコン一式（`icon-192/512.png`、`icon-maskable-512.png`、`apple-touch-icon.png`、`favicon-32.png`、`icon.svg`）。

- Service Worker は HTTPS（または localhost）でのみ動作。`file://` では PWA 機能は無効になり、その場合は単一ファイル版として動く。
- 更新時は `sw.js` の `CACHE` 版番号を上げると、次回オンライン起動で旧キャッシュが破棄され最新へ差し替わる（現行 `contrail-v2`）。

---

## 3. 画面・機能仕様

起動時に**スプラッシュ**（ロゴ演出）を挟み、**入り口画面**へ遷移。入り口でルート（出発地・到着地）を決め、4つのモードに分岐する。ヘッダーのロゴでいつでも入り口へ戻れる（演出を再生）。

### 3.0 入り口（ルート選択）

| 項目 | 内容 |
| --- | --- |
| UI | 搭乗券カード（ROUTE）に FROM / TO の都市プルダウン。空港コードを大きく表示 |
| 選択方法 | 地域ごとにグループ分けしたネイティブのプルダウン（§5）。切り取り線に隠れない実装 |
| 入れ替え | FROM / TO の間の**⇄ボタン**で出発地・到着地をワンタップで入れ替え |
| 共有 | 選択したルートは Mode A / Mode B へ引き継がれ、再入力不要。到着地（TO）は Mode C の見出しにも表示 |
| 表示 | 各都市の IATA コード・都市名・現在のUTCオフセットを補足表示 |

### 3.1 Mode A｜所要時間

| 項目 | 内容 |
| --- | --- |
| 片道／往復 | 上部トグルで切替。**往復**にすると復路（TO→FROM）の搭乗券が追加表示され、往路・復路の所要時間を同時計算。復路ルートは往路の逆で自動連動。切替時も入力済み日時は保持 |
| 入力 | 出発日時（出発地の現地時刻）、到着日時（到着地の現地時刻）。日付＋時刻 |
| 出力 | 所要時間（○時間○分）を搭乗券の切り取り線下に大きく表示。都市間時差も併記 |
| 入れ替え | ルート中央の**⇄ボタン**で出発地・到着地を入れ替え（結果も即再計算） |
| 付加 | 「現在地との時差」ストリップ（選択都市 vs 端末のタイムゾーン、現在時刻付き） |
| 自動計算 | 都市と日時が揃った時点で自動計算 |

### 3.2 Mode B｜クルーレスト分割

| 項目 | 内容 |
| --- | --- |
| 入力 | 基準タイムゾーン（出発地／到着地／UTC）、基準日、レスト開始・終了、交替数、引き継ぎ（分・任意） |
| 出力 | 各グループのレスト時間帯（開始–終了）と1グループあたりのレスト長。レスト全体の合計も表示 |
| 分割 | 総時間を交替数で均等割り（端数は §4.2 の規則で配分） |
| 併記 | 各枠を出発地／到着地／UTC の時刻で併記（入り口で選んだ都市を利用） |
| 都市変更 | 「都市を変更」から出発地・到着地を選び直せる（入り口のルートと共有） |
| 共有 | 計算後に**リンクをコピー／画像で保存・共有**（§4.4）。クルー同士で割り当てを共有できる |

### 3.3 Mode C｜到着前チェックリスト

| 項目 | 内容 |
| --- | --- |
| 目的 | 着陸前のクルー業務（CIQ・保安・ギャレー引き継ぎ・乗客対応）をチェック形式で確認 |
| 固定リスト | 3グループを内蔵。**CIQ・保安**（CIQ書類の配布・回収／機内消毒／Seal・Lock 確認／施錠）、**ギャレー・引き継ぎ**（冷却・保管／次便への引き継ぎ／残数カウント）、**客室・乗客対応**（到着アナウンス打ち合わせ／引き継ぎが必要な客への声かけ／預かり品返却／VIP挨拶） |
| 追加項目 | 「自分で追加」グループから任意の項目を追加（入力→＋追加 / Enter）。各項目はチェック・**×で削除**が可能 |
| 進捗 | 上部に進捗バーと「◯/◯ 完了」を表示。固定・追加の全項目を算入 |
| 到着地 | 上部に到着地（TO）の IATA・都市名を表示（入り口で選んだ都市を共有） |
| 目標時刻・カウントダウン | 「最終の着陸体制」の目標時刻を `datetime-local` で設定。到着地（TO）のタイムゾーン（未選択時はホーム）で解釈し、現在時刻までの残り時間を `H:MM:SS` で大きく表示。1秒ごとに更新（既存のライブ時計 tick に相乗り）。残り15分以下で警告色、超過で `00:00:00`＋超過メッセージ |
| 保存 | チェック状態・追加項目・目標時刻を端末に自動保存（§4.5）。「チェックをリセット」でチェックのみ解除（追加項目・目標時刻は残す） |

### 3.4 Mode D｜Atlas 連携（準備中 / Coming soon）

| 項目 | 内容 |
| --- | --- |
| 位置づけ | 入り口に幅いっぱいのタブとして配置。「COMING SOON」バッジ付き |
| 現状 | 未接続。タップすると「近日公開予定」と通知するのみ。機能は未実装 |
| 予定 | 到着地（TO）をキーに、姉妹アプリ **Atlas** のレイオーバー先おすすめスポット（グルメ等）を表示 |

---

## 4. 計算ロジック（本仕様の核心）

### 4.1 所要時間

所要時間は、**両方の現地時刻をUTCに変換してから引き算する**。

```
所要時間 = 到着時刻(UTC) − 出発時刻(UTC)
```

現地時刻からUTCへの変換は、その都市の IANA タイムゾーンで、**その日付・時刻時点のUTCオフセット**を使う（夏時間で変わるため固定値は持たない）。実装は `Intl.DateTimeFormat` の `formatToParts` で指定ゾーンの壁時計値を求め、UTC との差からオフセットを算出、DST境界を1回補正して確定する。

- 出発・到着とも**日付＋時刻**を入力させることで、翌日到着・日付変更線越え（到着日が見かけ上戻る）を自然に処理する。
- 結果が負（到着＜出発）の場合はエラー表示（§7）。

### 4.2 レスト分割

```
1グループのレスト長 = (総時間 − 引き継ぎ×(交替数−1)) ÷ 交替数
グループnの開始 = レスト開始 + (n−1)×(1グループ長 + 引き継ぎ)
グループnの終了 = グループnの開始 + 1グループ長
```

- レスト開始・終了は基準タイムゾーンの壁時計値として §4.1 と同じ方法で絶対時刻に変換。**終了 ≤ 開始なら翌日にまたぐ**ものとして +24h（例 22:00→02:00 = 4時間）。
- 均等割りで割り切れない場合、分単位に丸め、**余りを先頭グループから1分ずつ配分**する。
- 各グループの開始・終了を、出発地／到着地／UTC の各ゾーンに変換して併記する。

### 4.3 検証ケース

**所要時間**

| 出発 | 到着 | 期待 |
| --- | --- | --- |
| 東京 6/10 18:00 (UTC+9) | ニューヨーク 6/10 18:00 (UTC−4) | 13時間 |
| ロンドン 12/1 10:00 (UTC+0) | 東京 12/2 06:00 (UTC+9) | 11時間 |
| ニューヨーク 3/9 01:00（DST開始前, UTC−5） | ロサンゼルス 3/9 05:00（DST開始後, UTC−7） | **6時間** |

> 3番目はDST開始日をまたぐケース。両都市とも現地02:00で1時間進むため、単純計算の7時間ではなく6時間が正解。ここが正しく出ればロジックは健全。

**レスト分割**

| レスト全体 | 交替 | 引き継ぎ | 期待 |
| --- | --- | --- | --- |
| 10:00〜12:00 | 2 | 0分 | 各60分（10:00–11:00 / 11:00–12:00） |
| 22:00〜翌02:00 | 2 | 0分 | 各120分（日跨ぎ） |
| 10:00〜12:00 | 3 | 0分 | 各40分 |
| 10:00〜12:00 | 2 | 10分 | 各55分（間に引き継ぎ10分） |

### 4.4 共有（リンク／画像）

Mode B のレスト計算結果を、クルー同士で共有するための2手段。いずれも端末内で完結し、相手のアプリ・アカウントは不要。

**リンク共有**
- 現在の条件（出発地 `f`・到着地 `t`・基準TZ `b`・基準日 `d`・開始 `s`・終了 `e`・交替数 `n`・引き継ぎ `g`）をクエリ文字列に符号化した URL を生成。
- スマホでは Web Share API（共有シート）、非対応時はクリップボードへコピー。
- 受信側がその URL を開くと、パラメータを復元して **Mode B が同じ割り当てで開く**（起動時に `loadFromURL` が判定）。PWA としてデプロイ後は公開URLを基点にするため共有リンクもそのまま機能する。

**画像共有**
- レスト表を Canvas に搭乗券デザインで描画し、PNG を生成（各グループの時間帯を出発地／到着地／UTC 併記）。
- Web Share API（ファイル共有）対応時は共有シート、非対応時は PNG をダウンロード。

### 4.5 チェックリストの保存（Mode C）

- 固定項目はコード内の配列 `CHECKLIST`（グループ→項目）で保持。各項目のキーは `グループ番号-項目番号`。
- 追加項目は別の配列で保持し、`u` + 生成ID をキーにする。追加時に入力値を**エスケープしてから描画**（XSS対策）。
- チェック状態は `{キー: true}` の辞書として localStorage に保存。進捗（完了数／総数）は固定＋追加の全項目から算出。
- 「チェックをリセット」はチェック辞書のみ空にし、追加項目リスト・目標時刻は保持する。
- 目標時刻は `datetime-local` の値（`YYYY-MM-DDTHH:mm`）をそのまま保存。表示時に到着地（TO）のTZ（未選択時はホーム）の壁時計として instant に変換し、残り時間を算出。空にすると保存を削除。
- 保存キー: `contrail_checklist_v2`（チェック状態）、`contrail_ck_custom_v2`（追加項目）、`contrail_ck_target_v2`（目標時刻）。データ形式を変える際はキーの版番号を上げる。

---

## 5. データモデル（都市マスタ）

都市は内蔵配列で保持。1件が1空港・都市に対応する。

| フィールド | 例 | 用途 |
| --- | --- | --- |
| `c`（IATA） | HND, JFK | 表示・搭乗券コード |
| `n`（都市名） | 東京・羽田 | 表示・選択 |
| `z`（IANA） | Asia/Tokyo | 計算のキー（必須） |
| `g`（地域） | 日本／アジア／北米 等 | プルダウンのグループ分け |

- オフセットは保持せず、`z` から都度算出する。
- 現行は主要な就航都市を約45件収録（日本・アジア・中東・オセアニア・ヨーロッパ・北米・中南米）。追加は配列に1行足すだけ。

---

## 6. デザイン仕様

| 要素 | 内容 |
| --- | --- |
| シグネチャー | **搭乗券（ボーディングパス）**。FROM→TO を空港コードで表し、切り取り線を挟んで所要時間／レストを見せる |
| アプリアイコン | **時計＋軌道**モチーフ。ネイビー地に、クリーム色の時計盤＋子午線、破線の軌道（飛行機雲）と出発点のオレンジ点、右上にゴールドの機体。角はネイビーで全面塗りし、Android マスカブル版は安全域80%で中央配置 |
| タグライン | 起動画面に「時差・所要時間・クルーレスト・グルメ」 |
| パレット | ダークネイビー基調（`#1C2A3A`）＋温かみのある紙色（`#F4F0E8`）＋くすみ系アクセント（スレート `#3E5C76`／ブリック `#B5654A`／ゴールド `#B08A4F`） |
| タイポ | データ・コードは等幅（IBM Plex Mono）、本文は IBM Plex Sans。フォント未読込時はシステムフォントへフォールバック |
| 演出 | 起動スプラッシュで飛行機がアーチ（飛行機雲）を描き、ロゴが立ち上がる。ロゴ再タップで再生 |
| 操作系 | ルート中央の円形 ⇄ ボタン（ホバーで回転）、Mode A の「片道／往復」セグメント切替、Mode D の COMING SOON バッジ |
| チェックリスト | Mode C は搭乗券カード内にグループ見出し＋チェック行。チェックでスレート緑のボックス＋取り消し線、上部に進捗バー。追加行は入力欄＋「＋追加」、追加項目は右端に × 削除ボタン。到着地の下に目標時刻カード（`datetime-local`＋大きな等幅カウントダウン、残り15分で警告色・超過で警告色） |
| 都市選択 | 地域グループ付きネイティブプルダウン。搭乗券カードの `overflow:hidden`（切り欠き表現）に隠れない |
| フィードバック | 共有・準備中通知などはトースト表示 |
| アクセシビリティ | キーボード操作対応、スクリーンリーダー用の要約、`prefers-reduced-motion` で演出を抑制 |
| プライバシー | 現在地はタイムゾーンのみ利用。座標や個人データは送信・保存しない |

---

## 7. エラーハンドリング・エッジケース

| ケース | 挙動 |
| --- | --- |
| 到着 < 出発（所要時間） | 「到着が出発より前です」と表示、値は出さない |
| 都市・日時未入力 | 計算せず入力を促す |
| 現在地TZ取得不可 | 端末の既定タイムゾーンにフォールバック |
| DST開始で存在しない時刻 | オフセット補正に従い自然に処理 |
| レスト終了 ≤ 開始 | 翌日にまたぐものとして計算（正常扱い） |
| 交替数が未入力・0 | 1以上を促す |
| 引き継ぎ合計 ≥ レスト全体 | 「引き継ぎが長すぎます」と表示、分割しない |
| 割り切れない分割 | §4.2 の端数規則で分単位配分 |

---

## 8. 非機能要件

- **対応環境**: モダンブラウザ（Chrome / Safari / Firefox / Edge の最新版）。`Intl` のタイムゾーン機能に依存。
- **パフォーマンス**: 入力から結果表示まで体感即時。
- **保守性**: 計算・データ・UI が疎結合。都市追加や文言変更が容易。
- **移植性**: ロジックとデータは Next.js（Atlas 本体）へそのまま移植可能。

---

## 9. 今後の拡張

- **Mode D（Atlas 連携）の実装** — 到着地の IATA をキーに Atlas のおすすめスポット（グルメ等）を表示
- チェックリスト（Mode C）: 機種・路線別プリセット、順序の入れ替え、共有（リンク／画像）
- 到着時刻の逆算（出発時刻＋所要時間 → 到着ローカル時刻）
- 都市の検索（コード・名称）併用、お気に入り・履歴
- レスト: グループごとに異なる長さの手動編集、機体別プリセット、交替順の指定
- レスト開始・交替のリマインダー通知（PWA）
- 多言語化（現状は日本語UI固定）、ダーク／ライト切替
- フォント完全内蔵（`@font-face` の base64 埋め込み）による無ネット化

> 実装済み: FROM ⇄ TO 入れ替え、往復同時表示、レスト表のリンク／画像共有、到着前チェックリスト（Mode C・追加項目／目標時刻カウントダウン／自動保存）、オフライン・PWA、アプリアイコン。

---

## 10. 主要ファイル一覧

| ファイル | 役割 |
| --- | --- |
| `contrail.html` | 単一ファイル版アプリ（保存して使用） |
| `index.html` | PWA 版アプリ（manifest / Service Worker を読み込む。中身は同一） |
| `manifest.webmanifest` | アプリ名・アイコン・テーマ色・表示モード |
| `sw.js` | Service Worker（アプリ本体とフォントをキャッシュ） |
| `icon-192/512.png`, `icon-maskable-512.png`, `apple-touch-icon.png`, `favicon-32.png`, `icon.svg` | アプリアイコン各種 |
| `contrail_spec.md` | 本仕様書 |
| `README_contrail.md` | オフライン利用・デプロイ手順 |

---

## 11. 用語

| 用語 | 説明 |
| --- | --- |
| IANAタイムゾーンID | `Asia/Tokyo` のような地域識別子。DST切替ルールを内包する標準の時間帯定義 |
| UTCオフセット | UTCとの時差（例 UTC+9）。同一都市でもDSTで変動する |
| DST（夏時間） | 一部の国が夏季にUTCオフセットを+1する制度 |
| 日付変更線 | これを越えると暦日が変わる経線。到着日が見かけ上前後する要因 |
| クルーレスト | 長距離便で乗務員が交替で取る休憩。本アプリはその時間帯を交替数で分割する |
| 交替数 | レストを分け合うグループ数（例 2交替＝2グループ） |
| 引き継ぎ | 交替時にグループ間へ挟む時間 |
| PWA | Progressive Web App。ホーム画面に追加でき、オフライン起動が可能なWebアプリ |
| Service Worker | ページやリソースをキャッシュし、オフライン動作を担うブラウザの仕組み |
| マスカブルアイコン | Android で任意形状にマスクされる前提のアイコン。中央の安全域に主要素を収める |
